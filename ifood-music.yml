server:
  port: '${PORT:9797}'

spotify:
  endpoints:
    authentication: 'https://accounts.spotify.com/api/token'
    suggestions: 'https://api.spotify.com/v1/recommendations'
  access:
    id: e3804acbf8ca4440a33206c36f903f55
    secret: a344b44bf79f4d5eb20a4a54e8709afd

openweather:
  endpoints:
    currentWeather: 'https://api.openweathermap.org/data/2.5/weather'
  access:
    apikey: '2d0feff4ab38bbe9dc34faa9725c3e2d'
  units: 'metric'

resilience4j.circuitbreaker:
  instances:
    playListSuggestion:
      registerHealthIndicator: true
      slidingWindowSize: 10
      permittedNumberOfCallsInHalfOpenState: 3
      minimumNumberOfCalls: 20
      waitDurationInOpenState: 50s
      failureRateThreshold: 50
      eventConsumerBufferSize: 10

resilience4j.retry:
  instances:
    playListSuggestion:
      maxRetryAttempts: 3
      waitDuration: 10s
      enableExponentialBackoff: true
      exponentialBackoffMultiplier: 2
      retryExceptions:
        - org.springframework.web.client.HttpServerErrorException
        - java.io.IOException
      ignoreExceptions:
        - br.com.ifood.playlistsuggestion.exceptions.PlayListSuggestionException
        - br.com.ifood.playlistsuggestion.exceptions.UnknownCityException

resilience4j.bulkhead:
  instances:
    playListSuggestion:
      maxConcurrentCall: 10
      maxWaitDuration: 30ms    

resilience4j.ratelimiter:
  instances:
    playListSuggestion:
      limitForPeriod: 10
      limitRefreshPeriod: 1s
      timeoutDuration: 3s
      registerHealthIndicator: true
      eventConsumerBufferSize: 100
